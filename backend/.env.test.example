# backend/.env.test
#
# Environment overrides for tests. Copy and fill in real values.
# This file takes precedence over .env when running tests.
#
# IMPORTANT: Keep your CLERK_SECRET_KEY for a *development* Clerk instance.
# Creating sessions programmatically (required for tests) only works in dev mode.
#
# Usage:
#   cp backend/.env.test.example backend/.env.test
#   # fill in the values below
#   cd backend && pytest

# SQLite database file used by the test suite. It is created on demand and can
# be deleted at any time to start from a clean slate.
DATABASE_URL=sqlite+aiosqlite:///./test.db
# Development Clerk secret key used to create and verify test sessions.
CLERK_SECRET_KEY=
# Comma-separated frontend origins allowed during integration tests.
CORS_ORIGINS=
# Enables test-specific backend behavior and configuration.
ENVIRONMENT=
